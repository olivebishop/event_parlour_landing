# Atlasflow uses Railpack (Railway-compatible config-as-code).
# Force the Bun Dockerfile instead of npm auto-detection.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
